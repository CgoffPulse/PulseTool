/**
 * Cross-tool types only — types referenced by more than one app.
 * App-internal types stay app-local. Add here only when a second app needs it.
 *
 * For v1: nothing crosses tools yet. The social tool's `Client` may eventually
 * be referenced by the dev hub, at which point its type moves here.
 */

export {};
