{
  "name": "pulse-ecosystem",
  "version": "0.1.0",
  "private": true,
  "description": "Pulse — the agency operating engine. Consolidated monorepo (apps/pulse) with route groups for command, social, voice, crm, analytics, dev. Cross-tool code lives in packages/*.",
  "scripts": {
    "dev": "pnpm --filter @pulse/pulse dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm --filter @pulse/pulse test",
    "migrate": "pnpm --filter @pulse/db migrate",
    "check:migrations": "pnpm --filter @pulse/db check"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@9.0.0"
}
